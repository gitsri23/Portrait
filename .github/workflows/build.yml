name: Cinematic Camera Production Build

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: zulu
          java-version: 17

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      # 🚀 1. ఫ్రెష్ ఆండ్రాయిడ్ కోర్ క్రియేషన్
      - name: Recreate Android Project Core
        run: |
          rm -rf android
          flutter create . --project-name cinematic_camera --org com.example --platforms=android

      # 📦 2. MASTER PUBSPEC GENERATOR
      - name: Generate Clean Production Pubspec
        run: |
          rm -f pubspec.yaml
          cat << 'EOF' > pubspec.yaml
          name: cinematic_camera
          description: "Premium minimal cinematic portrait camera app."
          publish_to: 'none'
          version: 1.0.0+1

          environment:
            sdk: '>=3.0.0 <4.0.0'

          dependencies:
            flutter:
              sdk: flutter
            google_fonts: ^6.1.0
            permission_handler: ^11.3.1

          dev_dependencies:
            flutter_test:
              sdk: flutter
            flutter_lints: ^3.0.0

          flutter:
            uses-material-design: true
          EOF

      # 🛠️ 3. పర్ఫెక్ట్ పర్మిషన్స్ తో AndroidManifest.xml ఇంజెక్షన్ 
      - name: Inject AndroidManifest.xml
        run: |
          cat << 'EOF' > android/app/src/main/AndroidManifest.xml
          <manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.example.cinematic_camera">
              <uses-permission android:name="android.permission.CAMERA" />
              <uses-permission android:name="android.permission.RECORD_AUDIO" />
              <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="29" />
              <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
              <application android:label="Portrait Camera" android:name="${applicationName}" android:icon="@mipmap/ic_launcher" android:requestLegacyExternalStorage="true">
                  <activity android:name=".MainActivity" android:exported="true" android:launchMode="singleTop" android:theme="@style/LaunchTheme" android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode" android:hardwareAccelerated="true" android:windowSoftInputMode="adjustResize">
                      <meta-data android:name="io.flutter.embedding.android.NormalTheme" android:resource="@style/NormalTheme" />
                      <intent-filter>
                          <action android:name="android.intent.action.MAIN"/>
                          <category android:name="android.intent.category.LAUNCHER"/>
                      </intent-filter>
                  </activity>
                  <meta-data android:name="flutterEmbedding" android:value="2" />
              </application>
          </manifest>
          EOF

      # 🎥 4. CameraX Extensions (BOKEH) డిపెండెన్సీ ఇంజెక్షన్
      - name: Inject CameraX Native Dependencies
        run: |
          cat << 'EOF' >> android/app/build.gradle.kts
          
          dependencies {
              val cameraxVersion = "1.3.1"
              implementation("androidx.camera:camera-core:$cameraxVersion")
              implementation("androidx.camera:camera-camera2:$cameraxVersion")
              implementation("androidx.camera:camera-lifecycle:$cameraxVersion")
              implementation("androidx.camera:camera-view:$cameraxVersion")
              implementation("androidx.camera:camera-video:$cameraxVersion")
              
              // గూగుల్ ఆఫీషియల్ హార్డ్‌వేర్ బోకె ఎక్స్‌టెన్షన్స్
              implementation("androidx.camera:camera-extensions:$cameraxVersion")
          }
          EOF

      # 🧠 5. నేటివ్ ఆటో-బోకె (MainActivity.kt) కోడ్ ఇంజెక్షన్
      - name: Inject Custom Native Kotlin Code
        run: |
          mkdir -p android/app/src/main/kotlin/com/example/cinematic_camera
          cat << 'EOF' > android/app/src/main/kotlin/com/example/cinematic_camera/MainActivity.kt
          package com.example.cinematic_camera

          import android.content.ContentValues
          import android.provider.MediaStore
          import android.view.Surface
          import androidx.annotation.NonNull
          import androidx.camera.core.*
          import androidx.camera.extensions.ExtensionMode
          import androidx.camera.extensions.ExtensionsManager
          import androidx.camera.lifecycle.ProcessCameraProvider
          import androidx.camera.video.*
          import androidx.camera.video.VideoCapture
          import androidx.core.content.ContextCompat
          import androidx.lifecycle.LifecycleOwner
          import io.flutter.embedding.android.FlutterActivity
          import io.flutter.embedding.engine.FlutterEngine
          import io.flutter.plugin.common.MethodChannel
          import io.flutter.view.TextureRegistry
          import java.text.SimpleDateFormat
          import java.util.Locale
          import java.util.concurrent.ExecutorService
          import java.util.concurrent.Executors

          class MainActivity : FlutterActivity() {
              private val CHANNEL = "cinematic_camera/methods"
              private var textureEntry: TextureRegistry.SurfaceTextureEntry? = null
              private var cameraProvider: ProcessCameraProvider? = null
              private var lensFacing = CameraSelector.LENS_FACING_BACK
              private lateinit var cameraExecutor: ExecutorService
              
              private var videoCapture: VideoCapture<Recorder>? = null
              private var recording: Recording? = null

              override fun configureFlutterEngine(@NonNull flutterEngine: FlutterEngine) {
                  super.configureFlutterEngine(flutterEngine)
                  cameraExecutor = Executors.newSingleThreadExecutor()

                  MethodChannel(flutterEngine.dartExecutor.binaryMessenger, CHANNEL).setMethodCallHandler { call, result ->
                      when (call.method) {
                          "initCamera" -> {
                              textureEntry = flutterEngine.renderer.createSurfaceTexture()
                              result.success(textureEntry?.id())
                              startCamera()
                          }
                          "startRecording" -> {
                              startVideoRecording()
                              result.success(null)
                          }
                          "stopRecording" -> {
                              recording?.stop()
                              recording = null
                              result.success(null)
                          }
                          "setMode" -> result.success(null)
                          "switchCamera" -> {
                              lensFacing = if (CameraSelector.LENS_FACING_FRONT == lensFacing) CameraSelector.LENS_FACING_BACK else CameraSelector.LENS_FACING_FRONT
                              startCamera() 
                              result.success(null)
                          }
                          else -> result.notImplemented()
                      }
                  }
              }

              private fun startCamera() {
                  val cameraProviderFuture = ProcessCameraProvider.getInstance(this)
                  cameraProviderFuture.addListener({
                      try {
                          cameraProvider = cameraProviderFuture.get()

                          // 1. ఇన్‌స్టంట్ 16:9 వ్యూఫైండర్ బాక్స్
                          val preview = Preview.Builder()
                              .setTargetAspectRatio(AspectRatio.RATIO_16_9)
                              .build()
                          
                          val recorder = Recorder.Builder()
                              .setQualitySelector(QualitySelector.from(Quality.HIGHEST))
                              .build()
                          videoCapture = VideoCapture.withOutput(recorder)

                          textureEntry?.surfaceTexture()?.apply {
                              setDefaultBufferSize(1080, 1920)
                              val surface = Surface(this)
                              preview.setSurfaceProvider { request ->
                                  request.provideSurface(surface, cameraExecutor) {}
                              }
                          }

                          // 2. గూగుల్ ఎక్స్‌టెన్షన్స్ మేనేజర్ లోడింగ్
                          val extensionsManagerFuture = ExtensionsManager.getInstanceAsync(this, cameraProvider!!)
                          extensionsManagerFuture.addListener({
                              val extensionsManager = extensionsManagerFuture.get()
                              
                              var cameraSelector = CameraSelector.Builder().requireLensFacing(lensFacing).build()

                              // 3. ఫోన్ లో నేటివ్ బోకె (Portrait Blur) హార్డ్‌వేర్ ఉంటే దాన్ని ఆటోమెటిక్ గా ఆన్ చేస్తుంది
                              if (extensionsManager.isExtensionAvailable(cameraSelector, ExtensionMode.BOKEH)) {
                                  cameraSelector = extensionsManager.getExtensionEnabledCameraSelector(cameraSelector, ExtensionMode.BOKEH)
                              }

                              try {
                                  cameraProvider?.unbindAll()
                                  cameraProvider?.bindToLifecycle(this as LifecycleOwner, cameraSelector, preview, videoCapture)
                              } catch (e: Exception) {
                                  // సేఫ్టీ ఫాల్‌బ్యాక్
                                  cameraProvider?.unbindAll()
                                  cameraSelector = CameraSelector.Builder().requireLensFacing(lensFacing).build()
                                  cameraProvider?.bindToLifecycle(this as LifecycleOwner, cameraSelector, preview, videoCapture)
                              }
                          }, ContextCompat.getMainExecutor(this))

                      } catch (exc: Exception) {
                          // హార్డ్‌వేర్ సైలెంట్ ఎర్రర్ మేనేజ్‌మెంట్
                      }
                  }, ContextCompat.getMainExecutor(this))
              }

              private fun startVideoRecording() {
                  val videoCapture = this.videoCapture ?: return
                  val name = "Cinematic_" + SimpleDateFormat("yyyyMMdd_HHmmss", Locale.US).format(System.currentTimeMillis())
                  val contentValues = ContentValues().apply {
                      put(MediaStore.MediaColumns.DISPLAY_NAME, name)
                      put(MediaStore.MediaColumns.MIME_TYPE, "video/mp4")
                  }

                  val mediaStoreOutputOptions = MediaStoreOutputOptions
                      .Builder(contentResolver, MediaStore.Video.Media.EXTERNAL_CONTENT_URI)
                      .setContentValues(contentValues)
                      .build()

                  recording = videoCapture.output
                      .prepareRecording(this, mediaStoreOutputOptions)
                      .withAudioEnabled()
                      .start(ContextCompat.getMainExecutor(this)) { recordEvent ->
                          if (recordEvent is VideoRecordEvent.Finalize) {
                              if (recordEvent.hasError()) {
                                  recording?.close()
                                  recording = null
                              }
                          }
                      }
              }

              override fun onDestroy() {
                  super.onDestroy()
                  cameraExecutor.shutdown()
              }
          }
          EOF

      - name: Fetch and Verify Assets
        run: flutter pub get

      # 🚀 6. ప్రొడక్షన్ రిలీజ్ APK కంపైలేషన్
      - name: Compile Release APK (Safe Mode)
        run: flutter build apk --release --no-tree-shake-icons

      # 📦 7. GitHub Releases లోకి అప్‌లోడ్
      - name: Create Production GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v1.0.0
          name: Portrait Camera Release v1.0.0
          files: |
            build/app/outputs/flutter-apk/app-release.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
